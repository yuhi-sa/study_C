\subsection{main.\-c}

\begin{DoxyCode}
00001 \textcolor{comment}{/**  @file main.c}
00002 \textcolor{comment}{ *   @brief  文字列を反転させる}
00003 \textcolor{comment}{ *   @date   2016/11/24}
00004 \textcolor{comment}{ *   @author 佐伯雄飛}
00005 \textcolor{comment}{ *   @author B162392}
00006 \textcolor{comment}{ */}
00007 
00008 \textcolor{preprocessor}{#include <stdio.h>}
00009 \textcolor{comment}{}
00010 \textcolor{comment}{/** @fn int main(void)}
00011 \textcolor{comment}{ *  @brief 文字列を反転させる}
00012 \textcolor{comment}{ *}
00013 \textcolor{comment}{ *  入力：}
00014 \textcolor{comment}{ *  - 標準入力から（空白を含まない99文字以下の）文字列が与えられる．}
00015 \textcolor{comment}{ *}
00016 \textcolor{comment}{ *  出力：}
00017 \textcolor{comment}{ *  - 入力された文字を反転して表示する．}
00018 \textcolor{comment}{ *}
00019 \textcolor{comment}{ *  入力例：}
00020 \textcolor{comment}{\(\backslash\)verbatim}
00021 \textcolor{comment}{this\_is\_a\_pen}
00022 \textcolor{comment}{\(\backslash\)endverbatim}
00023 \textcolor{comment}{  *  出力例：}
00024 \textcolor{comment}{\(\backslash\)verbatim}
00025 \textcolor{comment}{nep\_a\_si\_siht}
00026 \textcolor{comment}{\(\backslash\)endverbatim}
00027 \textcolor{comment}{ *  入力例：}
00028 \textcolor{comment}{\(\backslash\)verbatim}
00029 \textcolor{comment}{72&o3mGOOLkjxQAbfHr/N>I&sDhPJ}
00030 \textcolor{comment}{\(\backslash\)endverbatim}
00031 \textcolor{comment}{  *  出力例：}
00032 \textcolor{comment}{\(\backslash\)verbatim}
00033 \textcolor{comment}{JPhDs&I>N/rHfbAQxjkLOOGm3o&27}
00034 \textcolor{comment}{\(\backslash\)endverbatim}
00035 \textcolor{comment}{ *  @date   2016/11/24}
00036 \textcolor{comment}{ *  @author 佐伯雄飛，B162392}
00037 \textcolor{comment}{ */}
00038 \textcolor{keywordtype}{int} main(\textcolor{keywordtype}{void}) \{
00039   \textcolor{keywordtype}{int} len = 100;
00040   \textcolor{keywordtype}{char} input[100];
00041 
00042   scanf(\textcolor{stringliteral}{"%99s"}, input);
00043 
00044   \textcolor{keywordtype}{int} i;
00045   \textcolor{keywordtype}{int} count = 0;
00046   \textcolor{keywordflow}{for} (i = 0; input[i] != \textcolor{charliteral}{'\(\backslash\)0'}; i++) \{
00047     count++;
00048   \}
00049   \textcolor{keywordflow}{for} (i = count - 1; i >= 0; i--) \{
00050     printf(\textcolor{stringliteral}{"%c"}, input[i]);
00051   \}
00052   printf(\textcolor{stringliteral}{"\(\backslash\)n"});
00053   \textcolor{keywordflow}{return} 0;
00054 \}
\end{DoxyCode}
