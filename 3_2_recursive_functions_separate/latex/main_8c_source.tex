\subsection{main.\-c}

\begin{DoxyCode}
00001 \textcolor{comment}{/**  @file main.c}
00002 \textcolor{comment}{ *   @brief  再帰関数を計算する}
00003 \textcolor{comment}{ *   @date   2016/11/10}
00004 \textcolor{comment}{ *   @author 佐伯雄飛}
00005 \textcolor{comment}{ *   @author B162392}
00006 \textcolor{comment}{ */}
00007 
00008 \textcolor{preprocessor}{#include <stdio.h>}
00009 
00010 \textcolor{keywordtype}{int} Fibonacci(\textcolor{keywordtype}{int} n);
00011 \textcolor{keywordtype}{int} factorial(\textcolor{keywordtype}{int} a);
00012 \textcolor{comment}{}
00013 \textcolor{comment}{/** @fn int main(void)}
00014 \textcolor{comment}{ *  @brief 再帰関数を計算する}
00015 \textcolor{comment}{ *}
00016 \textcolor{comment}{ *  入力：}
00017 \textcolor{comment}{ *  - 標準入力には，正の整数が1つ与えられる．}
00018 \textcolor{comment}{ *}
00019 \textcolor{comment}{ *  出力：}
00020 \textcolor{comment}{ *  - 与えられた整数nのフィボナッチ数と階乗を計算する}
00021 \textcolor{comment}{ *    関数を呼び出し，}
00022 \textcolor{comment}{ *    この順番で標準出力に表示する．}
00023 \textcolor{comment}{ *  - 数値と数値の間には空白を1ついれる}
00024 \textcolor{comment}{ *  - 実数xが正の整数ではない場合，}
00025 \textcolor{comment}{\(\backslash\)verbatim}
00026 \textcolor{comment}{Error: the number must be positive}
00027 \textcolor{comment}{\(\backslash\)endverbatim}
00028 \textcolor{comment}{ *    というエラーを表示し，（return 0で）終了する．}
00029 \textcolor{comment}{ *}
00030 \textcolor{comment}{ *  入力例：}
00031 \textcolor{comment}{\(\backslash\)verbatim}
00032 \textcolor{comment}{5}
00033 \textcolor{comment}{\(\backslash\)endverbatim}
00034 \textcolor{comment}{  *  出力例：}
00035 \textcolor{comment}{\(\backslash\)verbatim}
00036 \textcolor{comment}{5 120}
00037 \textcolor{comment}{\(\backslash\)endverbatim}
00038 \textcolor{comment}{  *  入力例：}
00039 \textcolor{comment}{\(\backslash\)verbatim}
00040 \textcolor{comment}{-2}
00041 \textcolor{comment}{\(\backslash\)endverbatim}
00042 \textcolor{comment}{  *  出力例：}
00043 \textcolor{comment}{\(\backslash\)verbatim}
00044 \textcolor{comment}{Error: the number must be positive}
00045 \textcolor{comment}{\(\backslash\)endverbatim}
00046 \textcolor{comment}{ *  @date   2016/11/10}
00047 \textcolor{comment}{ *  @author 佐伯雄飛，B162392}
00048 \textcolor{comment}{ */}
00049 \textcolor{keywordtype}{int} main(\textcolor{keywordtype}{void}) \{
00050   \textcolor{keywordtype}{int} n;
00051   scanf(\textcolor{stringliteral}{"%d"}, &n);
00052 
00053   \textcolor{keywordflow}{if} (n > 0) \{
00054     printf(\textcolor{stringliteral}{"%d "}, Fibonacci(n));
00055     printf(\textcolor{stringliteral}{"%d\(\backslash\)n"}, factorial(n));
00056 
00057   \} \textcolor{keywordflow}{else} \textcolor{keywordflow}{if} (n <= 0) \{
00058     printf(\textcolor{stringliteral}{"Error: the number must be positive"});
00059     printf(\textcolor{stringliteral}{"\(\backslash\)n"});
00060   \}
00061   \textcolor{keywordflow}{return} 0;
00062 \}
\end{DoxyCode}
