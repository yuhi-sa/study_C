\subsection{main.\-c}

\begin{DoxyCode}
00001 \textcolor{comment}{/**  @file main.c}
00002 \textcolor{comment}{ *   @brief  配列の平均の計算}
00003 \textcolor{comment}{ *   @date   2016/11/10}
00004 \textcolor{comment}{ *   @author 佐伯雄飛}
00005 \textcolor{comment}{ *   @author B162392}
00006 \textcolor{comment}{ */}
00007 
00008 \textcolor{preprocessor}{#include <stdio.h>}
00009 
00010 \textcolor{keywordtype}{float} average(\textcolor{keywordtype}{float} x[], \textcolor{keywordtype}{int} n);
00011 \textcolor{comment}{}
00012 \textcolor{comment}{/** @fn int main(void)}
00013 \textcolor{comment}{ *  @brief 配列の平均の計算}
00014 \textcolor{comment}{ *}
00015 \textcolor{comment}{ *  入力：}
00016 \textcolor{comment}{ *  - 標準入力の最初の整数はn（nは1以上の整数）}
00017 \textcolor{comment}{ *  - それに引き続くn個の実数はベクトルxの要素}
00018 \textcolor{comment}{ *    (\(\backslash\)f$x = (x\_1, x\_2, \(\backslash\)ldots, x\_n) \(\backslash\)f$)}
00019 \textcolor{comment}{ *}
00020 \textcolor{comment}{ *  出力：}
00021 \textcolor{comment}{ *  - x\_iの平均を計算する関数averageを呼び出し，出力する．}
00022 \textcolor{comment}{ *  - 数値は小数点第5位まで表示する（%.5f）}
00023 \textcolor{comment}{ *}
00024 \textcolor{comment}{ *  入力例：}
00025 \textcolor{comment}{\(\backslash\)verbatim}
00026 \textcolor{comment}{4 0.185748966526 0.32415795911 0.825689568236 0.371623309603}
00027 \textcolor{comment}{\(\backslash\)endverbatim}
00028 \textcolor{comment}{  *  出力例：}
00029 \textcolor{comment}{\(\backslash\)verbatim}
00030 \textcolor{comment}{0.42680}
00031 \textcolor{comment}{\(\backslash\)endverbatim}
00032 \textcolor{comment}{  *  入力例：}
00033 \textcolor{comment}{\(\backslash\)verbatim}
00034 \textcolor{comment}{2 0.808179641113 0.322645787577}
00035 \textcolor{comment}{\(\backslash\)endverbatim}
00036 \textcolor{comment}{  *  出力例：}
00037 \textcolor{comment}{\(\backslash\)verbatim}
00038 \textcolor{comment}{0.56541}
00039 \textcolor{comment}{\(\backslash\)endverbatim}
00040 \textcolor{comment}{ *  @date   2016/11/10}
00041 \textcolor{comment}{ *  @author 佐伯雄飛，B162392}
00042 \textcolor{comment}{ */}
00043 \textcolor{keywordtype}{int} main(\textcolor{keywordtype}{void}) \{
00044   \textcolor{keywordtype}{int} length;
00045   scanf(\textcolor{stringliteral}{"%d"}, &length);
00046 
00047   \textcolor{keywordtype}{float} x[length];
00048   \textcolor{keywordflow}{for} (\textcolor{keywordtype}{int} i = 0; i < length; i++) \{
00049     \textcolor{keywordtype}{float} element;
00050     scanf(\textcolor{stringliteral}{"%f"}, &element);
00051     x[i] = element;
00052   \}
00053 
00054   printf(\textcolor{stringliteral}{"%.5f\(\backslash\)n"}, average(x, length));
00055 
00056   \textcolor{keywordflow}{return} 0;
00057 \}
\end{DoxyCode}
